import React from 'react';
import { StoryEvent } from '../../types/events';

export default function EventCard({ event }: { event: StoryEvent }) {
  // CRUD, display, and edit functionality for individual story events will go here
  return <div></div>;
}
